kafka-lag-exporter {
  reporters.prometheus.port = 8000
  clusters = [
    {
      name = "kafka-cluster"
      bootstrap-brokers = "kafka:29092"
      labels = {
        location = "local"
        environment = "development"
      }
    }
  ]
}

